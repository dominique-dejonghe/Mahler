# Legacy: Mahler Reise (Hono / Cloudflare Pages)

The original public tour site + private prospecting app from `main` is preserved here.

It is **not** the Gustaaf atlas. Run Gustaaf from the repository root (`npm install && npm run dev`).

To run this legacy app locally you would need its original dependencies (`hono`, `wrangler`, etc.).
